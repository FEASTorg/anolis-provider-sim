# Chaos Layer

`chaos/` provides runtime fault-injection capabilities:

- `fault_injection.*`: fault state and injection primitives
- `chaos_control_device.*`: ADPP control surface for injected faults

This layer is optional for hardware providers but useful for resilience testing.
