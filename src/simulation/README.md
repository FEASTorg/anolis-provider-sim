# Simulation Layer

`simulation/` is provider-sim specific and can be removed for hardware providers.

- `simulation_engine.hpp`: engine abstraction
- `engines/`: inert, local, and remote simulation engines
- `adapters/`: external simulation protocol adapters (FluxGraph)
