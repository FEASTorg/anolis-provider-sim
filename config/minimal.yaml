# Minimal Configuration
# Only essential devices for lightweight testing

devices:
  - id: tempctl0
    type: tempctl
simulation:
  mode: inert  # No automatic updates for deterministic testing    initial_temp: 20.0

simulation:
  noise_enabled: false
  update_rate_hz: 5
